
Абсолютно верно! Отличное наблюдение! 

**Унарные методы** для управляющих операций:
- `register` - регистрация клиента
- `unregister` - отключение клиента  
- `joinGroup` / `leaveGroup` - управление группами
- `getStatus` - получение статуса/списка клиентов
- возможно `route` для разовых сообщений (если нужно)

**Двунаправленный стриминг** для основного обмена сообщениями:
- Клиент открывает bidirectional stream при подключении
- Через него идет постоянный обмен `RoutingMessage`
- Роутер пушит входящие сообщения через этот же стрим

Вот как это будет выглядеть:

```dart
class RouterResponderContract extends RpcResponderContract {
  RouterResponderContract() : super('Router') {
    // Унарные методы для управления
    addUnaryMethod<ClientRegistration, StatusMessage>(
      methodName: 'register',
      // ...
    );
    
    addUnaryMethod<GroupAction, StatusMessage>(
      methodName: 'joinGroup', 
      // ...
    );
    
    // Основной двунаправленный стрим для сообщений
    addBidirectionalMethod<RoutingMessage, RoutingMessage>(
      methodName: 'messageStream',
      handler: (incomingStream) {
        return _handleMessageStream(incomingStream);
      },
    );
  }
  
  Stream<RoutingMessage> _handleMessageStream(Stream<RoutingMessage> incoming) async* {
    await for (final message in incoming) {
      // Маршрутизируем входящее сообщение
      await _routeMessage(message);
      
      // Если нужно отправить что-то обратно отправителю
      if (message.type == RoutingType.DELIVERY_CONFIRMATION) {
        yield RoutingMessage.confirmation(message.id);
      }
    }
  }
}
```

**Серверный/клиентский стриминг** действительно не нужны, потому что:
- ❌ Server stream - мы не делаем "один запрос → поток ответов"
- ❌ Client stream - мы не аккумулируем "поток запросов → один ответ"
- ✅ Bidirectional - именно то что нужно для real-time чата/роутинга

Получается очень чистая архитектура: управление через unary, живое общение через bidirectional! 🎯
